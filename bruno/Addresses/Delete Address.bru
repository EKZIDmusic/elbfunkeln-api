meta {
  name: Delete Address
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/addresses/:id
  body: none
  auth: bearer
}

params:path {
  id: address-id-here
}

auth:bearer {
  token: {{token}}
}
