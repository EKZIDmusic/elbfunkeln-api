meta {
  name: Remove from Favorites
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/favorites/:productId
  body: none
  auth: bearer
}

params:path {
  productId: product-id-here
}

auth:bearer {
  token: {{token}}
}
