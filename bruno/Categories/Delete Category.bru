meta {
  name: Delete Category
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/categories/:id
  body: none
  auth: bearer
}

params:path {
  id: category-id-here
}

auth:bearer {
  token: {{token}}
}
