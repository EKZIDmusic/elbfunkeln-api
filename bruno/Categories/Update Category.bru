meta {
  name: Update Category
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/categories/:id
  body: json
  auth: bearer
}

params:path {
  id: category-id-here
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Updated Category"
  }
}
