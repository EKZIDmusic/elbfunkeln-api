meta {
  name: Remove Cart Item
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/cart/items/:id
  body: none
  auth: bearer
}

params:path {
  id: cart-item-id-here
}

auth:bearer {
  token: {{token}}
}
