meta {
  name: Create Order
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/orders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "shippingAddressId": "address-id-here",
    "paymentMethod": "CREDIT_CARD"
  }
}
