meta {
  name: Delete Product
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/products/:id
  body: none
  auth: bearer
}

params:path {
  id: product-id-here
}

auth:bearer {
  token: {{token}}
}
